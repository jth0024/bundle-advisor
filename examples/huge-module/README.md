# Huge Module Example

## Getting Started

1. Clone this repo
2. Install turbo -- `npm i -g turbo`
3. Install dependencies `pnpm install`
4. Run `turbo analyze:bundles`
5. Check the `bundle-reports/` directory